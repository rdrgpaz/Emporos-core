body: JSON.stringify({
  model: "gpt-4.1-mini",
  messages: [
    {
      role: "user",
      content: "Generate high-converting buyer intent ads for " + product + " in " + lang
    }
  ],
  max_tokens: 300
})
